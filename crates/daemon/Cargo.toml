[package]
name = "steward-daemon"
version = "0.1.0"
edition = "2021"

[dependencies]
steward-core = { path = "../core" }
tokio = { workspace = true }
tonic = { workspace = true }
anyhow = "1"
tracing = "0.1"
tracing-subscriber = "0.3"
rusqlite = { version = "0.32", features = ["bundled", "load_extension"] }
wasmtime = "21.0.0"

[dev-dependencies]
rusqlite = { version = "0.32", features = ["bundled", "load_extension"] }
