[package]
name = "steward-core"
version = "0.1.0"
edition = "2021"

[dependencies]
tonic = { workspace = true }
prost = { workspace = true }

[build-dependencies]
protoc-bin-vendored = "3.2.0"
tonic-build = { workspace = true }
